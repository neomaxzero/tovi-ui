import Styled from 'styled-components';

const StyledSection = Styled.div`
  width: 100%;
`;
export const CoolBackground = Styled.div`

   background: linear-gradient(
      rgba(255, 0, 0, 0.45), 
      rgba(255, 0, 0, 0.45)
    ),
    url(static/backgroundSearch.jpg);
`;
export default StyledSection;

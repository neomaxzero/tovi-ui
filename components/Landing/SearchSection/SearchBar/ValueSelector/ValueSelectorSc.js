import styled from 'styled-components';

export const ValueSelectorSc = styled.div`
  padding: 0.7em;
  padding-bottom: 0;
  padding-right: 2.5em;
`;

export const Title = styled.div`
  font-size: 0.8em;
  font-weight: bold;
  cursor: default;
`;

export const Value = styled.div`
  font-size: 1.4em;
  padding-top: 0.6em;
  padding-bottom: 0.7em;
`;